# ============================================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# NexPro - Professional Office Management System
# ============================================================
# WARNING: This is a template file. Copy to .env and fill in values
# NEVER commit .env file to version control
# ============================================================

# Django Settings
SECRET_KEY=<generate-strong-random-key-using-django-get_random_secret_key>
DEBUG=False
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com,api.yourdomain.com

# CORS Settings (Production)
CORS_ALLOW_ALL_ORIGINS=False
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Database Settings (PostgreSQL for Production)
USE_SQLITE=False
DB_NAME=nexpro_production
DB_USER=nexpro_db_user
DB_PASSWORD=<strong-random-password>
DB_HOST=your-db-host.example.com
DB_PORT=5432

# Email Settings (Production SMTP)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.yourprovider.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=noreply@yourdomain.com
EMAIL_HOST_PASSWORD=<email-app-password>
DEFAULT_FROM_EMAIL=NexPro <noreply@yourdomain.com>

# Celery Settings (Redis for Production)
CELERY_BROKER_URL=redis://your-redis-host:6379/0
CELERY_RESULT_BACKEND=redis://your-redis-host:6379/0

# Encryption Key (Generate using: from cryptography.fernet import Fernet; print(Fernet.generate_key().decode()))
FERNET_KEY=<your-fernet-key>

# Firm Settings
FIRM_NAME=Your Firm Name

# ============================================================
# IT ACT 2000 & DPDP ACT 2023 COMPLIANCE REQUIREMENTS
# ============================================================
#
# SECURITY CHECKLIST:
# [ ] SECRET_KEY is unique and not shared
# [ ] DEBUG is set to False
# [ ] ALLOWED_HOSTS includes only your domain(s)
# [ ] Database uses SSL connection
# [ ] HTTPS is enforced (handled by Django settings)
# [ ] Audit logs are enabled (automatic with production settings)
#
# DATA PROTECTION:
# [ ] Data encryption key (FERNET_KEY) is secure and backed up
# [ ] Backup encryption is enabled
# [ ] Data retention policies are configured
# [ ] User consent mechanisms are in place
#
# COMPLIANCE:
# [ ] Privacy Policy is published
# [ ] Terms of Service are published
# [ ] Grievance Officer contact is available
# [ ] Data Processing Agreement is available for B2B
#
# ============================================================
